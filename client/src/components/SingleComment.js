import React from "react";

const SingleComment = ({ comments = [] }) => {
  console.log(comments);
  
  // console.log(comment);
  return (
    <div>
      {/* <p>{comment}</p> */}
    </div>
  );
};

export default SingleComment;
